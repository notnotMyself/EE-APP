# Chris 设计评审员 - 技能清单

本文档定义了 Chris 设计评审员 的所有可用技能及其接口规范。

---

## 技能概览

| 技能 ID | 名称 | 说明 | 类型 |
|---------|------|------|------|
| `interaction_check` | 交互可用性检查 | 模式 A：生成交互可用性评审 prompt | Prompt 模板 |
| `visual_consistency` | 视觉一致性检查 | 模式 B：生成视觉一致性检查 prompt | Prompt 模板 |
| `compare_designs` | 方案对比 | 模式 C：生成多方案对比分析 prompt | Prompt 模板 |
| `vision_analysis` | 视觉分析 | 通用视觉分析（支持三种模式） | Prompt 模板 |
| `search_cases` | 知识库索引 | 获取知识库结构和搜索建议 | 知识索引 |

---

## 技能设计理念

```
┌─────────────────────────────────────────────────────────────┐
│                      Agent 内置能力                          │
│  👁️ 视觉分析    📖 Read    🔍 Grep    📁 Glob    ✍️ Write   │
└─────────────────────────────────────────────────────────────┘
                              ↑
                    Skills 提供业务指南
┌─────────────────────────────────────────────────────────────┐
│  📋 评审标准（5个检查维度）                                   │
│  📍 知识索引（去哪里找规范和案例）                            │
│  📝 输出格式（统一的报告模板）                                │
└─────────────────────────────────────────────────────────────┘
```

**Skills 只提供评审指南和 Prompt 模板，实际分析使用 Agent 自身的视觉能力和文件操作能力。**

---

## 1. interaction_check - 交互可用性检查

### 概述

生成交互可用性评审的标准化 Prompt，指导 Agent 从 5 个维度分析设计稿。

### 脚本位置

```
.claude/skills/interaction_check.py
```

### 调用方式

```bash
echo '<JSON参数>' | python3 .claude/skills/interaction_check.py
```

### 输入格式 (stdin JSON)

| 字段 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| `design_context` | string | ❌ | - | 设计背景（如"移动端登录页面"） |
| `core_task` | string | ❌ | - | 核心任务（如"用户完成账号登录"） |
| `specific_concerns` | string | ❌ | - | 特别关注的问题 |

### 检查维度

| 维度 | 说明 | 检查要点 |
|------|------|----------|
| **功能入口** | 用户认知模型匹配度 | 入口位置、视觉呈现、标签含义 |
| **操作路径** | 心智模型与断点检测 | 流程连贯性、步骤合理性、可逆性 |
| **交互一致性** | 平台规范符合度 | iOS HIG / Material Design |
| **状态反馈** | 操作确认充分度 | 加载/成功/失败状态、二次确认 |
| **认知负荷** | 复杂度控制 | 信息量、决策点、术语理解 |

### 输出格式

```json
{
  "success": true,
  "prompt": "# 交互可用性评审任务\n...",
  "params": {
    "design_context": "...",
    "core_task": "...",
    "specific_concerns": "..."
  }
}
```

### 使用示例

```bash
# 基本调用
echo '{"design_context": "移动端登录页面", "core_task": "用户完成账号登录"}' | python3 .claude/skills/interaction_check.py

# 无参数调用（获取默认 prompt）
echo '' | python3 .claude/skills/interaction_check.py
```

---

## 2. visual_consistency - 视觉一致性检查

### 概述

生成视觉一致性检查的标准化 Prompt，指导 Agent 检查设计的层级、一致性和潜在误导性。

### 脚本位置

```
.claude/skills/visual_consistency.py
```

### 调用方式

```bash
echo '<JSON参数>' | python3 .claude/skills/visual_consistency.py
```

### 输入格式 (stdin JSON)

| 字段 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| `design_context` | string | ❌ | - | 设计背景 |
| `specific_concerns` | string | ❌ | - | 特别关注的问题 |

### 检查维度

| 维度 | 说明 | 检查标准 |
|------|------|----------|
| **颜色使用** | 品牌色板、对比度 | WCAG AA >= 4.5:1 |
| **字体字号** | Type Scale | 最小字号 >= 12pt |
| **间距布局** | 8pt Grid | 间距为 8 的倍数 |
| **视觉层级** | 主次信息 | 关键操作是否突出 |
| **组件一致性** | 设计系统复用 | 同类元素样式统一 |

### 使用示例

```bash
echo '{"design_context": "商品详情页"}' | python3 .claude/skills/visual_consistency.py
```

---

## 3. compare_designs - 方案对比

### 概述

生成多方案对比分析的标准化 Prompt，指导 Agent 从专家视角评估各方案的可用性与体验差异。

### 脚本位置

```
.claude/skills/compare_designs.py
```

### 调用方式

```bash
echo '<JSON参数>' | python3 .claude/skills/compare_designs.py
```

### 输入格式 (stdin JSON)

| 字段 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| `design_context` | string | ❌ | - | 设计背景 |
| `core_task` | string | ❌ | - | 核心任务 |
| `user_persona` | string | ❌ | - | 目标用户画像 |
| `schemes` | array | ❌ | - | 方案名称列表（如 ["方案A", "方案B"]） |

### 评估维度

| 维度 | 说明 |
|------|------|
| **认知难度** | 理解设计意图的认知成本 |
| **操作效率** | 完成任务的步骤数和复杂度 |
| **决策负荷** | 需要做出的选择和判断 |
| **符合预期** | 心智模型和平台规范匹配度 |
| **心理负担** | 可能产生的焦虑或困惑 |

### 使用示例

```bash
echo '{"design_context": "首页改版", "schemes": ["方案A-卡片式", "方案B-列表式"]}' | python3 .claude/skills/compare_designs.py
```

---

## 4. vision_analysis - 视觉分析（通用）

### 概述

通用视觉分析 Prompt 生成器，支持三种模式（交互/视觉/对比）。

### 脚本位置

```
.claude/skills/vision_analysis.py
```

### 调用方式

```bash
echo '<JSON参数>' | python3 .claude/skills/vision_analysis.py
```

### 输入格式 (stdin JSON)

| 字段 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| `mode` | string | ❌ | `interaction` | 分析模式（见下表） |
| `design_context` | string | ❌ | - | 设计背景 |
| `core_task` | string | ❌ | - | 核心任务 |
| `specific_concerns` | string | ❌ | - | 特别关注的问题 |
| `user_persona` | string | ❌ | - | 目标用户画像 |

### 分析模式

| Mode | 说明 | 对应模式 |
|------|------|----------|
| `interaction` | 交互可用性验证 | 模式 A |
| `visual` | 视觉一致性检查 | 模式 B |
| `comparison` | 方案对比分析 | 模式 C |

### 使用示例

```bash
# 交互可用性分析
echo '{"mode": "interaction", "design_context": "登录页面"}' | python3 .claude/skills/vision_analysis.py

# 视觉一致性分析
echo '{"mode": "visual", "design_context": "设置页面"}' | python3 .claude/skills/vision_analysis.py

# 方案对比
echo '{"mode": "comparison", "design_context": "首页改版"}' | python3 .claude/skills/vision_analysis.py
```

---

## 5. search_cases - 知识库索引

### 概述

获取知识库的目录结构和搜索建议，指导 Agent 使用 Read/Grep 查阅参考资料。

### 脚本位置

```
.claude/skills/search_cases.py
```

### 调用方式

```bash
echo '<JSON参数>' | python3 .claude/skills/search_cases.py
```

### 输入格式 (stdin JSON)

| 字段 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| `action` | string | ❌ | `index` | 操作类型（`index` 或 `search`） |
| `query` | string | ❌ | - | 搜索关键词（action=search 时必填） |
| `category` | string | ❌ | - | 搜索范围（design_decisions/case_studies 等） |

### Action 类型

| Action | 说明 | 输出 |
|--------|------|------|
| `index` | 获取知识库结构 | 目录结构、文件列表、搜索建议 |
| `search` | 生成搜索 Prompt | 搜索建议和 Grep 命令示例 |

### 知识库结构

```
knowledge_base/
├── INDEX.md                     # 总索引（先读这个）
├── design_guidelines/           # 设计规范
│   ├── interaction-guidelines.md   # 交互规范 ⭐
│   └── visual-guidelines.md        # 视觉规范 ⭐
├── design_decisions/            # 历史设计决策 (ADR)
│   ├── INDEX.md
│   └── 001-xxx.md
├── case_studies/                # 历史案例
│   ├── INDEX.md
│   └── case-xxx.md
└── user_feedback/               # 用户反馈
    └── INDEX.md
```

### 使用示例

```bash
# 获取知识库索引
echo '' | python3 .claude/skills/search_cases.py

# 搜索登录相关案例
echo '{"action": "search", "query": "登录"}' | python3 .claude/skills/search_cases.py

# 在特定目录搜索
echo '{"action": "search", "query": "密码", "category": "case_studies"}' | python3 .claude/skills/search_cases.py
```

### 输出格式（index）

```json
{
  "success": true,
  "action": "index",
  "data": {
    "knowledge_base_path": "/path/to/knowledge_base",
    "structure": {
      "design_guidelines": {
        "description": "设计规范（交互规范、视觉规范）",
        "path": "/path/to/design_guidelines",
        "files": ["interaction-guidelines.md", "visual-guidelines.md"]
      },
      ...
    },
    "search_tips": [
      "使用 Read 工具直接读取规范文件...",
      "使用 Grep 搜索关键词..."
    ]
  }
}
```

---

## 风险等级标准

所有评审 Prompt 都使用统一的风险等级标准：

| 等级 | 标准 | 示例 |
|------|------|------|
| 🔴 **高** | 严重阻碍用户完成核心任务 | 关键按钮不可点击、流程断裂无法继续 |
| 🟡 **中** | 增加用户困惑或操作成本 | 入口位置不符预期、缺少状态反馈 |
| 🟢 **低** | 影响体验但不阻碍任务完成 | 文案不清晰、动效缺失 |

---

## 输出格式标准

所有评审报告中的问题都使用统一格式：

| 项目 | 内容 |
|------|------|
| **问题描述** | [具体描述发现的问题] |
| **风险等级** | 🔴 高 / 🟡 中 / 🟢 低 |
| **涉及页面/元素** | [具体的页面名称或 UI 元素] |
| **对用户体验的影响** | [说明该问题如何影响用户完成任务] |
| **改进建议** | [可操作的改进方案] |

---

## 典型使用流程

```
1. 接收设计稿图片和背景信息
   ↓
2. 确定评审模式
   - 模式 A: interaction_check.py
   - 模式 B: visual_consistency.py
   - 模式 C: compare_designs.py
   ↓
3. 调用对应 skill 获取评审 Prompt
   echo '{"design_context": "..."}' | python3 .claude/skills/xxx.py
   ↓
4. 读取设计规范
   Read knowledge_base/design_guidelines/interaction-guidelines.md
   ↓
5. 使用视觉能力分析设计稿
   ↓
6. 搜索历史案例作为参考
   grep -r "关键词" knowledge_base/case_studies/
   ↓
7. 按格式输出评审报告
   ↓
8. 如有重要决策，记录到知识库
   Write knowledge_base/design_decisions/XXX-xxx.md
```

---

## 注意事项

### ✅ Skills 的作用

- 提供标准化的评审 Prompt
- 定义检查维度和评判标准
- 指导 Agent 去哪里找参考资料
- 统一输出格式

### ❌ Skills 不做的事

- 不替代 Agent 的视觉分析能力
- 不自己实现 RAG 检索（让 Agent 自己查找）
- 不做硬编码的业务判断
- 不直接返回分析结果

