# ========================================
# AI数字员工平台 - 环境变量配置示例
# ========================================
#
# 使用说明:
# 1. 复制此文件为 .env (开发环境) 或 .env.production (生产环境)
# 2. 填入实际的配置值
# 3. 不要将包含敏感信息的 .env 文件提交到 Git
#
# ========================================

# 环境标识
# 值: dev | production
# 开发环境会显示详细错误信息，生产环境会隐藏技术细节
ENV=dev

# ========================================
# Supabase 配置
# ========================================

# Supabase 项目 URL
# 示例: https://your-project-id.supabase.co
SUPABASE_URL=https://dwesyojvzbltqtgtctpt.supabase.co

# Supabase Service Role Key (服务端密钥，拥有完整权限)
# 获取方式: Supabase Dashboard > Settings > API > Service Role Key
# ⚠️ 警告: 这是高权限密钥，绝不要暴露到客户端或公开代码库
SUPABASE_SERVICE_KEY=your-service-role-key-here

# 或者使用这个环境变量名（两者选其一）
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ========================================
# Anthropic Claude API 配置
# ========================================

# Claude API 密钥
# 获取方式: https://console.anthropic.com/
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# 或者使用自定义 Auth Token（适用于网关代理）
ANTHROPIC_AUTH_TOKEN=your-auth-token-here

# Claude API Base URL（可选，使用代理网关时配置）
# 留空则使用官方 API: https://api.anthropic.com
ANTHROPIC_BASE_URL=https://llm-gateway.oppoer.me

# 默认使用的模型（可选）
ANTHROPIC_MODEL=sonnet
ANTHROPIC_DEFAULT_SONNET_MODEL=claude-sonnet-4.5
ANTHROPIC_DEFAULT_HAIKU_MODEL=claude-haiku-4.5
ANTHROPIC_DEFAULT_OPUS_MODEL=claude-opus-4.5

# ========================================
# 极光推送 (JPush) 配置
# ========================================

# JPush App Key
# 获取方式: 极光推送控制台 > 应用设置 > 应用信息
# 申请地址: https://www.jiguang.cn/
JPUSH_APP_KEY=your-jpush-app-key-here

# JPush Master Secret
# 获取方式: 极光推送控制台 > 应用设置 > 应用信息
JPUSH_MASTER_SECRET=your-jpush-master-secret-here

# JPush APNS 环境（iOS推送）
# 值: true (生产环境) | false (开发环境)
JPUSH_APNS_PRODUCTION=false

# ========================================
# Gemini API 配置（封面图生成，可选）
# ========================================

# Google Gemini API 密钥
# 获取方式: https://aistudio.google.com/app/apikey
# 用于生成简报封面图片
GEMINI_API_KEY=your-gemini-api-key-here

# ========================================
# 应用配置
# ========================================

# 日志级别
# 值: DEBUG | INFO | WARNING | ERROR
# 生产环境建议使用 INFO 或 WARNING
LOG_LEVEL=INFO

# 是否显示第三方库的详细日志
# 值: 0 (不显示) | 1 (显示)
# 生产环境建议设为 0
APP_VERBOSE_LIB_LOGS=0

# API 请求超时时间（毫秒）
# 默认: 30000 (30秒)
API_TIMEOUT_MS=30000

# ========================================
# CORS 配置
# ========================================

# 允许的前端域名（逗号分隔）
# 开发环境: *（允许所有）
# 生产环境: 填入实际的前端域名
CORS_ORIGINS=*

# 生产环境示例:
# CORS_ORIGINS=https://app.eeplatform.com,https://www.eeplatform.com

# ========================================
# 数据库连接配置（可选，直接连接PostgreSQL）
# ========================================

# 如果不使用 Supabase SDK，可以配置直接连接字符串
# 格式: postgresql://user:password@host:port/database
# DATABASE_URL=postgresql://postgres:password@db.project.supabase.co:5432/postgres

# ========================================
# 其他配置
# ========================================

# Claude Code 相关（可选）
CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC=false

# ========================================
# 生产环境特别注意事项
# ========================================
#
# 1. 密钥安全:
#    - 使用强密码生成器生成密钥
#    - 定期轮换密钥（建议每90天）
#    - 使用密钥管理服务（如 AWS Secrets Manager）
#
# 2. 环境变量:
#    - 不要在代码中硬编码密钥
#    - 使用环境变量或配置文件
#    - 确保 .env 在 .gitignore 中
#
# 3. 数据库:
#    - 启用 SSL 连接
#    - 限制数据库访问 IP
#    - 定期备份数据库
#
# 4. 日志:
#    - 生产环境使用 INFO 级别
#    - 不要记录敏感信息（密码、Token等）
#    - 配置日志轮转和归档
#
# 5. CORS:
#    - 生产环境不要使用 * 通配符
#    - 只允许信任的前端域名
#
# 6. 监控:
#    - 配置应用性能监控（APM）
#    - 配置错误追踪（如 Sentry）
#    - 配置健康检查和告警
#
# ========================================
